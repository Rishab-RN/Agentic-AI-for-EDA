# Requirements for Intelligent Visualization Agent
# ================================================
# Install with: pip install -r requirements.txt

# Core Data Science
pandas>=1.5.0
numpy>=1.21.0
scipy>=1.9.0

# Visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# LangChain & LLM (Optional - for intelligent plot selection)
langchain>=0.1.0
langchain-groq>=0.1.0
langchain-core>=0.1.0

# LangGraph (Optional - for agentic workflow)
langgraph>=0.0.20

# Environment Variables
python-dotenv>=1.0.0

# Note: The module works in degraded mode if LangChain/LangGraph are not installed.
# Core functionality (plot generation) will still work using heuristics.
